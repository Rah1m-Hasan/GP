"use client"; import { BarChart3, Info } from "lucide-react"; import { skills } from "@/lib/demo-data"; import { useState } from "react";
export default function Skills(){const [category,setCategory]=useState('All');const cats=['All','Programming','Frameworks','Database','DevOps','Cloud','Problem Solving','Tools'];const list=skills.filter(s=>category==='All'||s.category===category);return <><p className="eyebrow">Skill analysis</p><h1 className="mt-2 text-3xl font-bold">Your skills, with useful context.</h1><p className="mt-2 muted">AI estimates are based on available profile evidence. Assessments provide stronger verification.</p><div className="mt-6 flex flex-wrap gap-2">{cats.map(c=><button onClick={()=>setCategory(c)} key={c} className={`rounded-full px-3 py-2 text-sm font-bold ${category===c?'bg-brand text-white':'bg-white border border-line'}`}>{c}</button>)}</div><div className="mt-7 grid gap-6 xl:grid-cols-[1.25fr_.75fr]"><section className="card overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-[#f4f7f6] text-xs uppercase tracking-wide muted"><tr><th className="p-4">Skill</th><th>Current</th><th>Required</th><th>Gap</th><th>Evidence</th></tr></thead><tbody>{list.map(s=>{const gap=s.current-s.required;return <tr key={s.name} className="border-t border-line"><td className="p-4 font-bold">{s.name}<span className="mt-1 block text-xs font-normal muted">{s.category}</span></td><td>{s.current}%</td><td>{s.required}%</td><td className={gap>=0?'text-brand':'text-[#b0662d]'}>{gap>=0?'+':''}{gap}%</td><td><span className="rounded bg-[#f1f5f4] px-2 py-1 text-xs">{s.source}</span></td></tr>})}</tbody></table></div></section><section className="card p-6"><h2 className="font-bold">Readiness by category</h2>{[['Programming',80],['Tools',75],['Database',55],['Frameworks',55],['DevOps',30],['Cloud',15]].map(([n,v])=><div key={String(n)} className="mt-5"><div className="flex justify-between text-sm"><b>{n}</b><span>{v}%</span></div><div className="progress mt-2"><span style={{width:`${v}%`}}/></div></div>)}<div className="mt-7 rounded-xl bg-mint p-4 text-sm"><Info size={16} className="mb-2 text-brand"/><b>Recommended focus</b><p className="mt-1 muted">Docker is your highest-leverage next step for backend job readiness.</p></div></section></div></>}
