"use client"; import Link from "next/link"; import { useParams, useRouter } from "next/navigation"; import { ArrowLeft, ArrowRight, CheckCircle2, MapPin, Sparkles } from "lucide-react"; import { jobs } from "@/lib/demo-data"; import { useState } from "react";
export default function JobDetail(){const {id}=useParams<{id:string}>();const job=jobs.find(j=>j.id===id)||jobs[0];const r=useRouter();const [applied,setApplied]=useState(false);const [roadmap,setRoadmap]=useState(false);return <><Link href="/dashboard/jobs" className="inline-flex items-center gap-2 text-sm font-bold text-brand"><ArrowLeft size={16}/>Back to jobs</Link><div className="mt-6 grid gap-6 xl:grid-cols-[1.3fr_.7fr]"><section><div className="card p-6"><div className="flex flex-wrap justify-between gap-4"><div><span className="grid h-12 w-12 place-items-center rounded-xl bg-mint text-lg font-bold text-brand">{job.company[0]}</span><h1 className="mt-5 text-3xl font-bold">{job.title}</h1><p className="mt-2 muted">{job.company} · {job.location} · {job.mode}</p></div><div className="text-right"><b className="text-lg">{job.salary}</b><p className="text-sm muted">{job.experience}</p></div></div><div className="mt-7 border-t border-line pt-6"><h2 className="font-bold">About the role</h2><p className="mt-3 leading-7 muted">{job.description} You&apos;ll collaborate with product and engineering teams to deliver secure, observable services with clean interfaces.</p><h2 className="mt-7 font-bold">Responsibilities</h2><ul className="mt-3 list-inside list-disc space-y-2 text-sm leading-6 muted"><li>Design and ship well-documented Python APIs.</li><li>Collaborate on technical decisions and pragmatic delivery.</li><li>Improve service reliability, testing, and developer experience.</li></ul><h2 className="mt-7 font-bold">Required skills</h2><div className="mt-3 flex flex-wrap gap-2">{job.skills.map(x=><span key={x} className="rounded-md bg-[#f1f5f4] px-3 py-1.5 text-sm">{x}</span>)}</div></div></div></section><aside className="space-y-5"><section className="card overflow-hidden"><div className="bg-[#183735] p-6 text-white"><p className="text-xs font-bold text-[#bde6dc]">YOUR JOB MATCH</p><div className="mt-2 flex items-end gap-2"><b className="text-5xl">{job.match}%</b><span className="mb-2 text-sm text-white/70">Match</span></div><p className="mt-3 text-sm text-white/75">A strong foundation. Close a few high-impact gaps to become more competitive.</p></div><div className="p-6"><h3 className="text-sm font-bold">Strong match</h3><div className="mt-3 space-y-2">{job.strong.map(x=><p key={x} className="flex items-center gap-2 text-sm"><CheckCircle2 size={16} className="text-brand"/>{x}</p>)}</div><h3 className="mt-5 text-sm font-bold">Skill gaps to address</h3><div className="mt-3 flex flex-wrap gap-2">{job.gaps.map(x=><span key={x} className="rounded-full bg-[#fff5e9] px-3 py-1 text-xs font-bold text-[#9a5b1d]">{x}</span>)}</div>{roadmap?<div className="mt-5 rounded-xl bg-mint p-4"><b className="text-sm">Roadmap generated</b><p className="mt-1 text-xs muted">Docker Fundamentals → AWS Foundations → System Design practice</p><Link href="/dashboard/learning" className="mt-3 inline-flex text-sm font-bold text-brand">Open learning path <ArrowRight size={14}/></Link></div>:<button onClick={()=>setRoadmap(true)} className="btn btn-secondary mt-6 w-full"><Sparkles size={16}/>Improve My Match</button>}<button onClick={()=>{setApplied(true);localStorage.setItem('nova-application','applied')}} className="btn btn-primary mt-3 w-full">{applied?'Application saved':'Apply Now'}</button></div></section></aside></div></>}
