import { Award, CheckCircle2, Lock, Trophy } from "lucide-react"; const badges=[['First Assessment','Completed your first skills assessment',true],['Python Verified','Scored 91% in Python',true],['SQL Verified','Scored 84% in SQL',true],['First Application','Saved a role to your tracker',true],['10 Learning Sessions','Build a consistent practice habit',false],['Interview Ready','Complete a mock interview',false],['Backend Ready','Reach 80% readiness',false]];
export default function Achievements(){return <><p className="eyebrow">Achievements</p><h1 className="mt-2 text-3xl font-bold">Progress you can point to.</h1><p className="mt-2 muted">Professional milestones that reflect evidence and consistent progress.</p><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{badges.map(([name,desc,earned])=><article key={name as string} className={`card p-6 ${earned?'':'opacity-65'}`}><span className={`grid h-12 w-12 place-items-center rounded-2xl ${earned?'bg-mint':'bg-[#eef1f0]'}`}>{earned?<Award className="text-brand"/>:<Lock className="muted"/>}</span><h2 className="mt-5 font-bold">{name as string}</h2><p className="mt-2 text-sm muted">{desc as string}</p><p className={`mt-4 text-xs font-bold ${earned?'text-brand':'muted'}`}>{earned?'Earned':'In progress'}</p></article>)}</div></>}
