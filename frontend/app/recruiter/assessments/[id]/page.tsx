import Link from 'next/link'; import { ArrowLeft, BarChart3, UsersRound } from 'lucide-react'; import { assessments, candidates } from '@/features/recruiter/data';
export default function AssessmentDetail({params}:{params:{id:string}}){const a=assessments.find(x=>x.id===+params.id)||assessments[0];return <><Link href="/recruiter/assessments" className="inline-flex items-center gap-2 text-sm font-bold text-brand"><ArrowLeft size={16}/>Assessments</Link><h1 className="mt-6 text-3xl font-bold">{a.title}</h1><p className="mt-2 muted">{a.job} · {a.questions} questions · {a.status}</p><div className="mt-7 grid gap-4 sm:grid-cols-3"><div className="card p-5"><p className="muted text-sm">Candidates assigned</p><b className="mt-2 block text-3xl">{a.assigned}</b></div><div className="card p-5"><p className="muted text-sm">Average score</p><b className="mt-2 block text-3xl text-brand">{a.average}%</b></div><div className="card p-5"><p className="muted text-sm">Evidence status</p><b className="mt-2 block text-lg">Recruiter review</b></div></div><section className="card mt-6 p-6"><h2 className="font-bold">Candidate results</h2><div className="mt-4 space-y-3">{candidates.filter(c=>c.assessment).slice(0,3).map(c=><div key={c.id} className="flex items-center justify-between rounded-xl border border-line p-4"><span><b>{c.name}</b><small className="ml-2 muted">Objective answers scored deterministically</small></span><b className="text-brand">{c.assessment}%</b></div>)}</div><p className="mt-5 text-sm muted">Subjective comments are clearly AI-assisted and require recruiter review.</p></section></>}
