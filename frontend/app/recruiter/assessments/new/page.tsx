"use client";
import { Check, Sparkles } from 'lucide-react'; import { recruiterJobs } from '@/features/recruiter/data'; import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function NewAssessment(){const [generated,setGenerated]=useState(false),[skills,setSkills]=useState('REST API, SQL, FastAPI, Docker, System Design');const r=useRouter();return <><p className="eyebrow">Create assessment</p><h1 className="mt-2 text-3xl font-bold">Build a role-specific evidence check.</h1><section className="card mt-7 max-w-3xl p-6"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold">Associated job<select className="mt-2 w-full rounded-xl border border-line bg-white p-3 font-normal">{recruiterJobs.map(j=><option key={j.id}>{j.title}</option>)}</select></label><label className="text-sm font-bold">Assessment title<input defaultValue="Backend API Foundations" className="mt-2 w-full rounded-xl border border-line p-3 font-normal"/></label><label className="text-sm font-bold">Difficulty<select className="mt-2 w-full rounded-xl border border-line bg-white p-3 font-normal"><option>Intermediate</option><option>Beginner</option><option>Advanced</option></select></label><label className="text-sm font-bold">Duration<input defaultValue="45 minutes" className="mt-2 w-full rounded-xl border border-line p-3 font-normal"/></label></div><label className="mt-4 block text-sm font-bold">Skills tested<input value={skills} onChange={e=>setSkills(e.target.value)} className="mt-2 w-full rounded-xl border border-line p-3 font-normal"/></label><div className="mt-5 flex gap-3"><button onClick={()=>setGenerated(true)} className="btn btn-secondary"><Sparkles size={16}/>Generate with AI</button><button onClick={()=>r.push('/recruiter/assessments')} className="btn btn-primary">Save draft</button></div>{generated&&<div className="mt-6 rounded-xl bg-mint p-5"><p className="eyebrow">Editable AI draft</p><h2 className="mt-2 font-bold">6 job-specific questions generated</h2><div className="mt-4 space-y-2 text-sm">{skills.split(',').slice(0,5).map((s,i)=><div key={s} className="rounded-lg bg-white p-3"><b>{i+1}. </b>What is a sound practical approach to {s.trim()} in a production backend service?</div>)}</div><p className="mt-4 text-sm muted"><Check size={15} className="mr-1 inline text-brand"/>Review and edit every question before publishing. AI has not evaluated any candidate.</p></div>}</section></>}
