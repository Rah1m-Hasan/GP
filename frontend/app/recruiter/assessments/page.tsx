import Link from 'next/link'; import { ClipboardCheck, Copy, Plus } from 'lucide-react'; import { assessments } from '@/features/recruiter/data'; import { PageHeading } from '@/features/recruiter/ui';
export default function Assessments(){return <><PageHeading eyebrow="Assessment management" title="Assessments" description="Job-specific assessments collect structured, relevant evidence." action={<Link href="/recruiter/assessments/new" className="btn btn-primary"><Plus size={16}/>Create assessment</Link>}/><div className="mt-7 grid gap-4">{assessments.map(a=><article key={a.id} className="card flex flex-wrap items-center justify-between gap-4 p-5"><div className="flex gap-4"><span className="grid h-11 w-11 place-items-center rounded-xl bg-mint"><ClipboardCheck className="text-brand"/></span><span><Link href={`/recruiter/assessments/${a.id}`} className="font-bold hover:text-brand">{a.title}</Link><p className="mt-1 text-sm muted">{a.job} · {a.questions} questions · {a.skills.join(', ')}</p></span></div><div className="flex items-center gap-6 text-sm"><span><b className="block">{a.assigned}</b><small className="muted">Assigned</small></span><span><b className="block">{a.average}%</b><small className="muted">Average</small></span><span className="rounded-full bg-mint px-2.5 py-1 text-xs font-bold text-brand">{a.status}</span><button title="Duplicate assessment"><Copy size={16}/></button></div></article>)}</div></>}
