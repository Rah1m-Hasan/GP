"use client";
import Link from 'next/link'; import { Copy, MoreHorizontal, Pause, Plus, Search } from 'lucide-react'; import { recruiterJobs } from '@/features/recruiter/data'; import { PageHeading } from '@/features/recruiter/ui'; import { useState } from 'react';
export default function RecruiterJobs(){const [tab,setTab]=useState('All'),[q,setQ]=useState('');const jobs=recruiterJobs.filter(j=>(tab==='All'||j.status===tab)&&j.title.toLowerCase().includes(q.toLowerCase()));return <><PageHeading eyebrow="Job management" title="Jobs" description="Create structured roles and keep your hiring team aligned." action={<Link href="/recruiter/jobs/new" className="btn btn-primary"><Plus size={16}/>Create job</Link>}/><div className="mt-7 flex flex-wrap gap-3"><div className="flex flex-1 items-center gap-2 rounded-xl border border-line bg-white px-3"><Search size={16} className="muted"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search jobs" className="w-full py-3 outline-none"/></div><div className="flex rounded-xl border border-line bg-white p-1">{['All','Draft','Active','Paused','Closed'].map(x=><button onClick={()=>setTab(x)} key={x} className={`rounded-lg px-3 py-2 text-sm font-bold ${tab===x?'bg-mint text-brand':'muted'}`}>{x}</button>)}</div></div><div className="card mt-5 overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-[#f4f7f6] text-xs uppercase tracking-wide muted"><tr><th className="p-4">Job</th><th>Location</th><th>Status</th><th>Applications</th><th>Qualified</th><th>Assessments</th><th>Interviews</th><th></th></tr></thead><tbody>{jobs.map(j=><tr className="border-t border-line" key={j.id}><td className="p-4"><Link href={`/recruiter/jobs/${j.id}`} className="font-bold hover:text-brand">{j.title}</Link><span className="mt-1 block text-xs muted">{j.department} · {j.type}</span></td><td>{j.location}</td><td><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${j.status==='Active'?'bg-mint text-brand':'bg-[#f1f4f3] text-[#667471]'}`}>{j.status}</span></td><td>{j.applications}</td><td>{j.qualified}</td><td>{j.assessments}</td><td>{j.interviews}</td><td><div className="flex gap-2"><Link href={`/recruiter/jobs/${j.id}`} className="text-xs font-bold text-brand">View</Link><button title="Duplicate job" className="muted"><Copy size={15}/></button><button title="Pause job" className="muted"><Pause size={15}/></button></div></td></tr>)}</tbody></table></div></>}
