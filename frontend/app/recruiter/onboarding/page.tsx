"use client";
import { Building2, CheckCircle2, ChevronRight, UserRound } from 'lucide-react'; import { useRouter } from 'next/navigation'; import { useState } from 'react';
const roles=['Software Engineering','Frontend','Backend','Full Stack','Data Science','Machine Learning','Cybersecurity','DevOps','Product','Design'];
export default function RecruiterOnboarding(){const [step,setStep]=useState(1),[selected,setSelected]=useState(['Backend','DevOps']);const r=useRouter();return <div className="mx-auto max-w-2xl py-5"><div className="mb-6 flex justify-between"><b>Nova Systems <span className="text-brand">Recruiter setup</span></b><span className="text-sm muted">Step {step} of 4</span></div><div className="progress mb-7"><span style={{width:`${step*25}%`}}/></div><section className="card p-6 sm:p-9">{step===1&&<><p className="eyebrow">Recruiter details</p><h1 className="mt-2 text-3xl font-bold">Welcome, Sarah.</h1><p className="mt-2 muted">Tell candidates who is guiding the hiring process.</p><div className="mt-6 grid gap-4 sm:grid-cols-2">{[['Full name','Sarah Johnson'],['Job title','Senior Technical Recruiter'],['Work email','sarah@novasystems.demo'],['Phone (optional)','+880 1712 000000']].map(([l,v])=><label key={l} className="text-sm font-bold">{l}<input defaultValue={v} className="mt-2 w-full rounded-xl border border-line px-3 py-3 font-normal"/></label>)}</div></>}{step===2&&<><p className="eyebrow">Company</p><h1 className="mt-2 text-3xl font-bold">Set up Nova Systems.</h1><div className="mt-6 grid gap-4 sm:grid-cols-2">{[['Company name','Nova Systems'],['Website','https://novasystems.demo'],['Industry','Financial technology'],['Headquarters','Dhaka, Bangladesh']].map(([l,v])=><label key={l} className="text-sm font-bold">{l}<input defaultValue={v} className="mt-2 w-full rounded-xl border border-line px-3 py-3 font-normal"/></label>)}</div><label className="mt-4 block text-sm font-bold">Company size<select className="mt-2 w-full rounded-xl border border-line bg-white p-3 font-normal">{['1–10','11–50','51–200','201–500','501–1000','1000+'].map(x=><option key={x} selected={x==='51–200'}>{x}</option>)}</select></label><label className="mt-4 block text-sm font-bold">Description<textarea defaultValue="Nova Systems builds dependable financial workflow tools for ambitious teams." className="mt-2 min-h-24 w-full rounded-xl border border-line p-3 font-normal"/></label></>}{step===3&&<><p className="eyebrow">Hiring preferences</p><h1 className="mt-2 text-3xl font-bold">What are you hiring for?</h1><div className="mt-6 grid gap-2 sm:grid-cols-2">{roles.map(x=><button key={x} onClick={()=>setSelected(s=>s.includes(x)?s.filter(v=>v!==x):[...s,x])} className={`rounded-xl border p-3 text-left text-sm font-semibold ${selected.includes(x)?'border-brand bg-mint text-brand':'border-line'}`}>{x}</button>)}</div><label className="mt-5 block text-sm font-bold">Hiring volume<select className="mt-2 w-full rounded-xl border border-line bg-white p-3 font-normal"><option>1–5</option><option>6–20</option><option>21–50</option><option>50+</option></select></label></>}{step===4&&<div className="text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-mint"><CheckCircle2 className="text-brand"/></span><p className="eyebrow mt-5">Ready to hire thoughtfully</p><h1 className="mt-2 text-3xl font-bold">Your recruiter workspace is ready.</h1><p className="mx-auto mt-3 max-w-md muted">Jobs, evidence-based candidate profiles, assessments, interviews, and structured decision support are all ready for Sarah at Nova Systems.</p></div>}<div className="mt-8 flex justify-between"><button onClick={()=>setStep(Math.max(1,step-1))} className={`btn btn-secondary ${step===1?'invisible':''}`}>Back</button><button onClick={()=>step===4?r.push('/recruiter/dashboard'):setStep(step+1)} className="btn btn-primary">{step===4?'Open workspace':'Continue'}<ChevronRight size={16}/></button></div></section></div>}
