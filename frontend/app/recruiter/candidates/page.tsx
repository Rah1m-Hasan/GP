"use client";
import { Search, SlidersHorizontal } from 'lucide-react'; import { candidates, recruiterJobs, stages } from '@/features/recruiter/data'; import { CandidateCard, PageHeading } from '@/features/recruiter/ui'; import { useState } from 'react';
export default function Candidates(){const [q,setQ]=useState(''),[stage,setStage]=useState('');const found=candidates.filter(c=>(c.name+c.role+c.skills.map(s=>s.name).join(' ')).toLowerCase().includes(q.toLowerCase())&&(!stage||c.stage===stage));return <><PageHeading eyebrow="Candidate directory" title="Candidates" description="Review only job-relevant evidence shared with the application."/><div className="mt-7 flex flex-wrap gap-3"><label className="flex flex-1 items-center gap-2 rounded-xl border border-line bg-white px-3"><Search size={17} className="muted"/><input value={q} onChange={e=>setQ(e.target.value)} className="w-full py-3 outline-none" placeholder="Search candidate, role, or skill"/></label><select value={stage} onChange={e=>setStage(e.target.value)} className="rounded-xl border border-line bg-white px-3 text-sm"><option value="">All stages</option>{stages.map(s=><option key={s}>{s}</option>)}</select><select className="rounded-xl border border-line bg-white px-3 text-sm"><option>All jobs</option>{recruiterJobs.map(j=><option key={j.id}>{j.title}</option>)}</select></div><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{found.map(c=><CandidateCard key={c.id} candidate={c}/>)}{!found.length&&<div className="card col-span-full p-10 text-center"><b>No candidates match your filters.</b><p className="mt-2 text-sm muted">Broaden the skill, stage, or job filters.</p></div>}</div></>}
