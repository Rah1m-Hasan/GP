"use client";
import { Bell, ShieldCheck } from 'lucide-react'; import { useState } from 'react'; import { PageHeading } from '@/features/recruiter/ui';
export default function Settings(){const [saved,setSaved]=useState(false);return <><PageHeading eyebrow="Workspace settings" title="Sarah Johnson" description="Senior Technical Recruiter · Nova Systems"/><section className="card mt-7 max-w-2xl p-6"><h2 className="font-bold">Notification preferences</h2>{['New applications','Assessment completions','Upcoming interviews','Jobs closing soon'].map(x=><label key={x} className="mt-4 flex items-center justify-between border-b border-line pb-4 text-sm"><span>{x}<small className="mt-1 block muted">Receive in-product notification</small></span><input defaultChecked type="checkbox" className="h-4 w-4 accent-brand"/></label>)}<h2 className="mt-7 font-bold">Decision-support guardrails</h2><p className="mt-2 text-sm leading-6 muted"><ShieldCheck size={16} className="mr-1 inline text-brand"/>AI summaries are limited to job-relevant evidence. It cannot automate ranking, rejection, selection, or hiring decisions.</p><button onClick={()=>setSaved(true)} className="btn btn-primary mt-6">Save settings</button>{saved&&<span className="ml-3 text-sm text-brand">Saved</span>}</section></>}
