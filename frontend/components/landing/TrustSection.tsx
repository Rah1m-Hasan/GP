import { BadgeCheck, Eye, LockKeyhole, Scale, ShieldCheck, UserRoundCheck } from "lucide-react";
const items = [[Eye, "Evidence over assumptions", "Skills are more meaningful with visible context."], [UserRoundCheck, "Human hiring decisions", "The recruiter remains accountable for the final call."], [BadgeCheck, "Transparent skill sources", "See whether a signal comes from a profile, project, or assessment."], [LockKeyhole, "Private career conversations", "Guidance belongs to the person doing the work."], [ShieldCheck, "Structured AI output", "Useful recommendations with clear boundaries."], [Scale, "Clear scoring logic", "Important readiness signals stay explainable."]];
export function TrustSection() { return <section className="section responsibility-section"><div className="shell"><div className="section-heading centered"><p className="section-kicker">Responsible career intelligence</p><h2>Trust is part of the product.</h2><p>Skillbridge is designed to make opportunity easier to understand — without replacing human context or judgment.</p></div><div className="responsibility-grid">{items.map(([Icon, title, copy]) => { const I = Icon as typeof Eye; return <article key={title as string}><I /><h3>{title as string}</h3><p>{copy as string}</p></article>; })}</div></div></section>; }
