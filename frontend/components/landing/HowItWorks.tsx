"use client";

import { useState } from "react";
const seeker = ["Build your career profile", "Discover skill gaps and matching roles", "Learn and prove what’s missing", "Apply and prepare for interviews"];
const recruiter = ["Create a job", "Review candidate evidence", "Assess and interview", "Make the hiring decision"];
export function HowItWorks() { const [audience, setAudience] = useState<"seeker" | "recruiter">("seeker"); const steps = audience === "seeker" ? seeker : recruiter; return <section className="section how-section" id="how-it-works"><div className="shell"><div className="section-heading centered"><p className="section-kicker">How it works</p><h2>Designed around the decisions that move work forward.</h2></div><div className="audience-toggle" role="tablist" aria-label="How it works audience"><button role="tab" aria-selected={audience === "seeker"} onClick={() => setAudience("seeker")}>For Job Seekers</button><button role="tab" aria-selected={audience === "recruiter"} onClick={() => setAudience("recruiter")}>For Recruiters</button></div><div className="how-steps">{steps.map((step, i) => <article key={step}><span>0{i + 1}</span><i /><h3>{step}</h3><p>{audience === "seeker" ? ["Bring your experience and goals together in one place.", "Understand the skills that most affect your readiness.", "Follow practical next steps and collect meaningful proof.", "Focus on roles where you can make a stronger case."][i] : ["Define the role, requirements, and evaluation approach.", "Review more than keywords with structured skill context.", "Use assessments and interview reports to build the full picture.", "Keep final judgment, context, and accountability human."][i]}</p></article>)}</div></div></section>; }
