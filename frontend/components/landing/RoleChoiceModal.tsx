"use client";

import Link from "next/link";
import { motion } from "framer-motion";
import { ArrowRight, BriefcaseBusiness, UserRound, X } from "lucide-react";
import { useEffect, useRef } from "react";
export function RoleChoiceModal({ close }: { close: () => void }) { const closeRef = useRef<HTMLButtonElement>(null); useEffect(() => { closeRef.current?.focus(); const key = (event: KeyboardEvent) => event.key === "Escape" && close(); document.addEventListener("keydown", key); return () => document.removeEventListener("keydown", key); }, [close]); return <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="role-modal-backdrop" role="presentation" onMouseDown={e => { if (e.target === e.currentTarget) close(); }}><motion.section initial={{ opacity: 0, scale: .97, y: 12 }} animate={{ opacity: 1, scale: 1, y: 0 }} exit={{ opacity: 0, scale: .97, y: 8 }} className="role-modal" role="dialog" aria-modal="true" aria-labelledby="role-choice-title"><button ref={closeRef} onClick={close} className="modal-close" aria-label="Close role selection"><X size={19} /></button><p className="section-kicker">Welcome to Skillbridge</p><h2 id="role-choice-title">How do you want to use Skillbridge AI?</h2><p className="modal-intro">Choose a workspace to begin. You can explore either experience in this demo.</p><div className="role-options"><article className="seeker-option"><UserRound /><h3>Job Seeker</h3><p>Discover skill gaps, build proof, and approach opportunities with a clearer plan.</p><Link href="/onboarding">Continue as Job Seeker <ArrowRight size={16} /></Link></article><article><BriefcaseBusiness /><h3>Recruiter</h3><p>Review skill evidence, manage candidates, and make better-informed hiring decisions.</p><Link href="/recruiter/coming-soon">Continue as Recruiter <ArrowRight size={16} /></Link></article></div></motion.section></motion.div>; }
