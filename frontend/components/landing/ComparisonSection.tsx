import { ArrowDown, ArrowRight, Check, X } from "lucide-react";
const before = ["Resume", "Apply", "Wait", "Rejection", "No explanation"];
const after = ["Profile", "Skill analysis", "Gap detection", "Learning", "Verification", "Better match", "Application"];
export function ComparisonSection() { return <section className="section comparison-section"><div className="shell"><div className="section-heading centered"><p className="section-kicker">A clearer way forward</p><h2>From guesswork to a visible path.</h2></div><div className="comparison-grid"><article className="comparison-card before-card"><div><span className="comparison-label"><X size={14} /> Without Skillbridge</span><p>A familiar loop with very little useful feedback.</p></div><div className="comparison-flow">{before.map((x, i) => <span key={x}>{x}{i < before.length - 1 && <ArrowDown />}</span>)}</div></article><article className="comparison-card after-card"><div><span className="comparison-label"><Check size={14} /> With Skillbridge</span><p>Every next action is connected to stronger evidence.</p></div><div className="comparison-flow">{after.map((x, i) => <span key={x}>{x}{i < after.length - 1 && <ArrowRight />}</span>)}</div></article></div></div></section>; }
