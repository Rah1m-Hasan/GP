import { Check, ChevronRight } from "lucide-react";
export function Metric({label,value,note}:{label:string;value:string;note:string}){return <div className="card p-5"><p className="text-sm font-medium muted">{label}</p><p className="mt-3 text-3xl font-bold">{value}</p><p className="mt-2 text-xs text-brand">{note}</p></div>}
export function SkillBar({name,current,required,verified=false}:{name:string;current:number;required:number;verified?:boolean}){return <div className="py-3"><div className="mb-2 flex justify-between text-sm"><span className="font-semibold">{name} {verified&&<span className="ml-1 rounded bg-mint px-1.5 py-0.5 text-[10px] text-brand">Verified</span>}</span><span className="muted">{current}% / {required}%</span></div><div className="progress"><span style={{width:`${current}%`}}/></div><div className="mt-1 text-right text-[11px] muted">Required {required}%</div></div>}
export function SectionTitle({title,action}:{title:string;action?:string}){return <div className="mb-4 flex items-center justify-between"><h2 className="text-lg font-bold">{title}</h2>{action&&<button className="flex items-center gap-1 text-sm font-bold text-brand">{action}<ChevronRight size={16}/></button>}</div>}
export function Status({children}:{children:React.ReactNode}){return <span className="inline-flex items-center gap-1 rounded-full bg-mint px-2.5 py-1 text-xs font-bold text-brand"><Check size={12}/>{children}</span>}
