"use client";

import Image from "next/image";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Bell, Building2, ChartNoAxesCombined, ChevronDown, ClipboardCheck, GitCompareArrows, LayoutDashboard, Menu, Search, Settings, UsersRound, BriefcaseBusiness, CalendarDays, X } from "lucide-react";
import { useState } from "react";
import { ThemeToggle } from "@/components/theme-toggle";

const nav = [[LayoutDashboard, "Dashboard", "/recruiter/dashboard"], [BriefcaseBusiness, "Jobs", "/recruiter/jobs"], [UsersRound, "Candidates", "/recruiter/candidates"], [ClipboardCheck, "Assessments", "/recruiter/assessments"], [CalendarDays, "Interviews", "/recruiter/interviews"], [GitCompareArrows, "Comparisons", "/recruiter/comparisons"], [ChartNoAxesCombined, "Analytics", "/recruiter/analytics"]] as const;
export default function RecruiterShell({ children }: { children: React.ReactNode }) { const [open, setOpen] = useState(false); const path = usePathname(); return <div className="min-h-screen bg-background lg:flex"><aside className={`${open ? "translate-x-0" : "-translate-x-full"} fixed inset-y-0 z-50 flex h-dvh w-[265px] flex-col border-r border-sidebar-border bg-sidebar p-5 text-sidebar-foreground transition-transform lg:sticky lg:top-0 lg:translate-x-0`}><div className="flex items-center justify-between"><Link href="/recruiter/dashboard" className="mb-7 flex items-center gap-2 font-bold"><Image src="/brand/logo.png" width={32} height={32} alt="" aria-hidden="true" />Skillbridge <em className="not-italic text-brand">AI</em></Link><button onClick={() => setOpen(false)} className="mb-7 p-1 lg:hidden" aria-label="Close navigation"><X size={18} /></button></div><p className="mb-2 px-3 text-[10px] font-bold uppercase tracking-[.14em] muted">Recruiter workspace</p><nav className="min-h-0 flex-1 space-y-1 overflow-y-auto">{nav.map(([Icon, name, href]) => <Link key={href} onClick={() => setOpen(false)} href={href} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold ${path === href || path.startsWith(`${href}/`) ? "bg-mint text-brand" : "text-muted-foreground hover:bg-muted"}`}><Icon size={18} />{name}</Link>)}</nav><div className="mt-3 shrink-0 border-t border-sidebar-border pt-3"><Link href="/recruiter/company" className={`mb-1 flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm ${path === "/recruiter/company" ? "bg-mint text-brand" : "text-muted-foreground hover:bg-muted"}`}><Building2 size={17} />Company</Link><Link href="/recruiter/settings" className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm ${path === "/recruiter/settings" ? "bg-mint text-brand" : "text-muted-foreground hover:bg-muted"}`}><Settings size={17} />Settings</Link><div className="mt-3 flex items-center gap-2 rounded-xl bg-muted p-2"><span className="grid h-8 w-8 place-items-center rounded-full bg-[#d9c2e8] text-xs font-bold">SJ</span><div className="text-xs"><b>Sarah Johnson</b><br /><span className="muted">Nova Systems</span></div></div></div></aside><main className="min-w-0 flex-1"><header className="sticky top-0 z-30 flex h-16 items-center gap-3 border-b border-border bg-background/90 px-4 backdrop-blur lg:px-8"><button onClick={() => setOpen(true)} className="lg:hidden" aria-label="Open navigation"><Menu /></button><div className="hidden flex flex-1 items-center gap-2 rounded-lg border border-border bg-card px-3 py-2 text-sm muted md:flex"><Search size={16} />Search jobs, candidates, assessments <span className="ml-auto text-xs">⌘ K</span></div><ThemeToggle /><button aria-label="Notifications" className="relative rounded-lg bg-card p-2"><Bell size={18} /><span className="absolute right-1 top-1 h-2 w-2 rounded-full bg-brand" /></button><button className="flex items-center gap-2 rounded-lg border border-border bg-card px-3 py-2 text-sm font-semibold">Nova Systems<ChevronDown size={15} /></button></header><div className="mx-auto max-w-[1500px] p-4 md:p-8">{children}</div></main></div>; }
